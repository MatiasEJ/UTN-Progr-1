#include <stdio.h>
int potencia(double x,int y)
{
 {

printf ("\nIntroduzca la base y el exponente: ");
scanf ("%lf%d", &x, &y);
double potencia=1;
for (int i=0; i<y; i++)

potencia = potencia * x; printf ("\nLa potencia es %.2.f", potencia);}
}

int main ()

{
    int a,b;
    potencia (a,b);
}

